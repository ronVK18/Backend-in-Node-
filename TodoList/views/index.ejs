<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="stylesheets/index.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="font-mono">
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form px-10 py-10 text-white">
        <form action="/create" method="post">
          <input
            class="block w-full rounded-lg mb-3 outline-none px-5 py-3 bg-zinc-800"
            type="text"
            placeholder="Title goes here ...."
            required
            name="title"
          />
          <textarea
            name="task"
            required
            class="block w-full rounded-lg mb-3 outline-none px-5 py-3 bg-zinc-800 resize-none"
            placeholder="Write your task details here ..."
          ></textarea>
          <input
            class="mt-2 bg-blue-600 px-5 py-2 rounded-md"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="tasks flex gap-3 flex-wrap p-10">
        <% if (file.length > 0) {%> <% file.forEach((task) => {%>
        <div class="task rounded-md min-w-72 px-3 py-4 bg-zinc-800">
          <h1 class="text-white text-2xl tracking-tighter font">
            <%= task %>
          </h1>
         
          <div class="mt-3 flex justify-between items-center">
            <a class="text-blue-500 inline-block " href="/file/<%= task %>">Read More</a>
            <a class="text-zinc-500 inline-block " href="/edit/<%= task %>">edit </a>
            <a class="text-red-500 bold inline-block " href="/delete/<%= task %>">  Delete </a>

          </div>
        </div>
        <%})%> <% } else { %>

        <h1 class="text-zinc-600">No tasks yet</h1>
        <% } %>
      </div>
    </div>
  </body>
</html>
